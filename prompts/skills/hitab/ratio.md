### "how many TIMES higher/larger is X than Y" → X / Y (X is the bigger one)
`answer = [str(X / Y)]` where X is the larger value. Mind numerator/denominator:
"how many times higher is A than B" = A / B. Keep full precision (≥6 sig figs),
do not round.
